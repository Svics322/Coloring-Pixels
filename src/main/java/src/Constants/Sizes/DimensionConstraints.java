package src.Constants.Sizes;

import java.awt.*;

public class DimensionConstraints {
    public static Dimension WHITESPACE = new Dimension(SizeConstraints.WHITESPACE_WIDTH, SizeConstraints.WHITESPACE_HEIGHT);
    public static final Dimension MINDIM = new Dimension(SizeConstraints.MIN_WIDTH, SizeConstraints.MIN_HEIGHT);
}
